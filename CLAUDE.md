@AGENTS.md

# Hub Financeiro — CLAUDE.md (fonte de verdade)

> **Autoridade:** este arquivo é a fonte de verdade do projeto; o Notion é espelho.
> Só fato verificado no código/runtime. Fato volátil leva **data de verificação**.
> Última revisão geral: **28/07/2026**.

## Visão geral

E-commerce de **panelas/cookware** (verificado: descrições de itens e custos em `PRODUTOS.xlsx`/`ml_custo_produto`), **multicanal**. O Hub responde, por **canal** e por **mês**: *quanto entrou de fato (líquido)* e *qual a margem de contribuição real* (faturamento → deduções → M.C.), e monta o **DRE consolidado** (receita → M.C. → despesas operacionais → resultado).

- Canais (verificado em `lib/data/supabase.ts` `CANAIS`): **Mercado Livre, Shopee, TikTok Shop, Amazon, SHEIN (MVP 07/08/2026 — credencial integrada, ingestão pendente), Magalu (MVP 17/08/2026 — OAuth + pedidos + card ativos), B2B** (id interno `vendas-internas`, nome de exibição "B2B").
- Despesas operacionais (abaixo da M.C.) vêm da **Omie** (contas a pagar + conta corrente).
- Regime tributário: **Lucro Real** (conforme `APRENDIZADOS_SHOPEE_ML.md`; consistente com PIS/COFINS + DIFAL no DRE — ver Integrações/Omie).
- **Argumento central do projeto:** os fechamentos manuais da equipe são **otimistas em 3 de 3 canais** (sempre na mesma direção). O Hub existe para medir o número real. Ver `LICOES.md`.

## Stack

- **Front:** Next.js **16.2.9**, React **19.2.4**, Tailwind **4**, Recharts (verificado em `package.json`). App Router, `app/` na raiz (sem `src/`).
- **Backend:** **Supabase Postgres** — **toda a lógica pesada vive no banco**: RPCs PL/pgSQL, extensão `extensions.http` (HTTP a partir do Postgres), `pg_cron` (agendador), **Vault** (`vault.decrypted_secrets`) para segredos. **113 migrations** em `supabase/migrations/` (verificado).
- **Edge Functions (Deno):** gateway fino de token e ingestão pesada (ex.: `ml-token` para refresh do ML). Não versionadas neste repo.
- Rodar local: `npm run dev` (porta 3000). `DATA_SOURCE=supabase` liga o provider real (ver Arquitetura de dados). Config em `.claude/launch.json`.

## Arquitetura de dados

### Camada `lib/data/`
- `index.ts` — seleciona o provider: `DATA_SOURCE === "supabase"` → `supabaseProvider`; **senão `mockProvider` (default)**. O `launch.json` já injeta `DATA_SOURCE=supabase`.
- `types.ts` — interface `DataProvider` e todos os tipos que a UI consome. Método obrigatório `getDashboard(month?)`; opcionais `getCronsStatus`, `getDreGrupoR`, `getDreGrupoRDetalhe`, `getDreItens`, `getDreTopo`, `getDreSku`.
- `supabase.ts` (~889 linhas) — provider real. Chama **RPCs** do Postgres via PostgREST com `SUPABASE_SERVICE_ROLE_KEY` (server-side). URL default hardcoded: `https://klwczmapuupensozxbsr.supabase.co` (não é segredo; overridável por `SUPABASE_URL`).
- `mock.ts` — dados fixos para desenvolvimento sem banco.

### RPCs (a lógica)
`getDashboard(mes)` monta o dashboard chamando um RPC por bloco/canal. Verificado em `supabase.ts`:
- **ML:** `ml_dashboard`, `ml_faturamento_ml`, `ml_comissao`, `ml_ads`, `ml_friccao`, `ml_frete`, `ml_full`, `ml_cmv`, `ml_dre_diario`; afiliados/DIFAL via `sp_afiliados_ml`/`sp_difal_ml`.
- **Shopee:** `sp_faturamento`, `sp_cmv`, `sp_afiliados`, `shopee_ads`, `shopee_difal`, `sp_custo_devolucoes`, `sp_repasse`.
- **TikTok:** `tt_faturamento`, `tt_deducoes`, `tt_cmv`.
- **Amazon:** `az_faturamento`, `az_deducoes`, `az_comissao`, `az_frete_mes`, `az_cmv`.
- **Magalu:** `magalu_faturamento`, `magalu_deducoes`, `magalu_cmv`.
- **B2B:** `b2b_faturamento`, `b2b_cmv`.
- **Série diária (gráfico):** `ml_dre_diario` (ML) + `dre_diario_canais` (demais canais).
- **DRE:** topo (Receita → M.C.) = `getDreTopo` (reusa `getDashboard`, consolida acima da M.C.); **Grupo R** (despesas) = `omie_dre_grupo_r` / `_detalhe` / `omie_dre_itens` (drill-down 3 níveis). Fonte do Grupo R: view `omie_dre_lancamentos`.
- **Saúde dos crons:** `crons_status`.

### CMV com vigência temporal (03/08/2026)
- `ml_custo_produto` tem **vigência por linha**: `[vigencia_inicio, vigencia_fim)`, `fim NULL` = vigente; PK `(sku, vigencia_inicio)` + constraint EXCLUDE (btree_gist) impede vigências sobrepostas do mesmo SKU. Linhas originais: `vigencia_inicio = 2000-01-01`.
- **Alteração de custo NUNCA é UPDATE no valor** — trigger bloqueia. Fluxo: `cmv_alterar_custo(sku, novo_custo, data_vigencia)` fecha a linha vigente e insere a nova (função revogada de anon/authenticated; rodar como service_role).
- As 9 RPCs que leem CMV (`ml_cmv`, `ml_cmv_cobertura`, `sp_cmv`, `tt_cmv`, `az_cmv`, `b2b_cmv`, `ml_dre_diario`, `dre_diario_canais`, `dre_sku`) fazem **range join ancorado na data de competência** de cada canal (ML `ml_pedidos.data`; Shopee/TT `create_time`@SP; AZ `purchase_date`; B2B `data_emissao`) — custo novo só afeta vendas de `vigencia_inicio` em diante; meses fechados não mudam.
- **Não-regressão:** `scripts/cmv-regression/` — `snapshot.mjs` captura as 9 RPCs × últimos 6 meses + margem por canal×mês; `diff.mjs` compara dois snapshots (meses fechados têm que bater exato). Rollback completo em `scripts/cmv-regression/rollback_cmv_vigencia.sql`. Migrations `20260803190001/190002`.

### pg_cron
**36 jobs ativos (verificado 18/08/2026).** Horários no cron são **UTC** (BRT = −3h). Ingestão por canal + keepalive de token + reconferências:
```
ml-diario (0 6)         ml-semanal (0 8 dom)     ml-refresh-token (*/30)
ml-ads-item (25 6)      ml-ads-reconferir (0 9)  ml_fatura_tarifas (30 4)
ml_full_ccolpa (40 4)
shopee-diario (30 6)    shopee-semanal (15 8 dom) shopee-token-keepalive (15 */3)
shopee-wallet (30 7)    shopee-wallet-tarde (10 16)
shopee-ciclo (0 8)      shopee-ciclo-tarde (40 16)
shopee-escrow-meiodia (0 15)  shopee-escrow-tarde (0 21)
shopee-ads-reconferir-meiodia (0 15)  shopee-ads-reconferir-tarde (0 21)
tt-diario (0 7)         tt-semanal (45 8 dom)    tt-token-keepalive (0 7)
shein-diario (50 6)     shein-semanal (35 8 dom)
az-diario (15 6)        az-semanal (30 8 dom)
az-recebiveis (40 7)    az-recebiveis-tarde (20 16)
azads-diario (45 6)     azads-colher (25 7)
magalu-diario (50 6)    magalu-token-keepalive (*/30)
mp-recebiveis (15 7)    mp-recebiveis-tarde (0 16)
omie-diario (0 8)       omie-semanal (30 8 dom)
tiny-token-keepalive (25 */4)
```
Monitorar: aba **`/crons`** (RPC `crons_status`) e tabela `oauth_refresh_log` (mensagens dos crons: `shopee_cron`, `tt_cron`, `omie_diario`, etc.).

## Onde as réguas vivem (mapa)

> **O que este arquivo é:** o CLAUDE.md é **inventário + decisões**, **não** um tratado de réguas. As **réguas fundas** (competência, status, escrow, deduções, pegadinhas de API) **não estão aqui** — vivem no código e no `APRENDIZADOS_SHOPEE_ML.md`. Este mapa só diz **onde cada uma vive**. Vai mexer numa régua? Siga o ponteiro e leia a fonte — não confie neste resumo. (Ponteiros verificados em 28/07/2026.)

- **Réguas ML** (venda `paid + partially_refunded`; competência `date_closed`@SP; custos de billing por `creation_date`/mês-calendário; ciclo 23→22): `APRENDIZADOS_SHOPEE_ML.md` §A3.1, §A3.2, §A3.5 + RPCs `ml_faturamento_ml`, `ml_comissao`, `ml_ads`, `ml_dre_diario`; cron `ml_cron_diario`. Deduções da M.C.: §A3.4.
- **`sale_fee` e o que NÃO entra** (CFONPN, CVVPRC/CVVFNU, estornos, CXDE): bloco "Billing ML — o que NÃO entra" acima. **BPAD credita o ADS** (ADS líquido): RPC `ml_ads` (comentário em `lib/data/supabase.ts`) + §A3.4.
- **Réguas Shopee** (bruta = não-cancelados; escrow = repasse desde a venda; ADS/DIFAL fora do escrow, na wallet): `APRENDIZADOS_SHOPEE_ML.md` §B3.2, §B3.4 + RPCs `sp_faturamento`, `sp_repasse`; cron `shopee_cron_diario` (fases lista→escrow→ADS→DIFAL); vazão do escrow em `shopee_fill_escrow`.
- **TikTok** (guard "não-liquidado" = `total_count=0` + array vazio; taxonomia de retry): função `tt_fill_finance`; cron `tt_cron_diario`; régua de faturamento (pago antes de liquidar) em `tt_faturamento` (ver §Integrações/TikTok).
- **Omie / DRE de despesas** (Grupo R; classificação projeto→categoria; PIS/COFINS/DIFAL): view `omie_dre_lancamentos` + RPC `omie_dre_grupo_r`; ingestão `omie_fill_despesas`. (Fora do escopo do APRENDIZADOS.)
- **B2B / Tiny** (discriminador `ecommerce` vazio + natureza `Venda%`): função `b2b_fill_notas`; leitura `b2b_faturamento` (ver §Integrações/B2B).
- **Pegadinhas de API** (ML billing trunca em silêncio, `detail_sub_types` plural, `document_type` obrigatório, `from_id`+ASC, `CURLOPT_TIMEOUT_MS`; Shopee dedup da wallet, caps de janela): `APRENDIZADOS_SHOPEE_ML.md` §A2.5, §A2.6 e §B2, §B2.1.
- **Pegadinhas NOVAS do billing ML (03/08/2026):** (a) o endpoint `/billing/integration/.../details` passou a rate-limitar agressivo (~31/07): HTTP 429 `local_rate_limited` já na 2ª chamada em rajada — todo caller precisa de backoff (nunca retry imediato); (b) **zero silencioso**: sob throttle a API pode responder **200 com `total=0` e `results` vazio** — a mesma query, minutos depois, devolve o total real. `total=0` com linhas já no banco = suspeito, re-ler antes de aceitar. Ambos tratados em `ml_fill_billing`/`ml_api_total` (migration `20260803120001_ml_billing_backoff_429`).
- **Fundações comuns** (HTTP de dentro do Postgres, custódia OAuth no Vault, advisory locks): `APRENDIZADOS_SHOPEE_ML.md` §0.3, §0.4, §0.5.

## Mudanças de 05/08/2026 (sessão de conferência do DRE — Luciano)

- **Crons diários resilientes:** cada fase/passo roda em subtransação com log de falha próprio — um timeout não descarta mais o dia inteiro (incidente shopee-diario 05/08, SSL timeout + rollback). Shopee `20260805140001`; ML/TT/AZ `150001`. `crons_status` ganhou `falhas_24h`/`ultima_falha`/`recuperado_em` e lê falhas de fase dos logs de app (`140002`/`150002`); UI em `CronsBoard.tsx`.
- **Omie AP — competência padrão = `data_entrada`** (a competência que o financeiro mantém na Omie), fallback emissão/vencimento — `200002`. Regras específicas prevalecem: PIS/COFINS (venc−1, inalterada); **Frete Flex** (projeto `11028072487` → C2, forca_inclusao; quinzena: doc "2ª%" = emissão−1 mês, "1ª" = mês da NF — régua do Luciano domina sobre a entrada) `190001`/`200001`; **parcelamento DIFAL BA** (fornecedor `10705160510`: sem categoria → assume 2.06.94/I1; competência = vencimento; entrada jun R$16k, nada em jul, 59×R$15.652,51 de 20/08/2026 a jun/2031) `160002`/`190002`; CABFORT parcelas (inalterada).
- **Órfãos Omie (soft-delete):** título que some da ListarContasPagar é carimbado em `ausente_desde` e sai do DRE (nunca deletado; volta se reaparecer) — `160001`. Causa raiz: baixas/renegociações re-lançam títulos com id novo.
- **`forca_inclusao` por projeto** (lançamento entra no DRE mesmo com categoria excluída): ligado SÓ em "Fretes Flex". Foi testado em "Marketing & Tráfego" e **REVERTIDO em ~30min** (dupla contagem ~R$294k: boletos de ads de plataforma usam esse projeto) — `180001` + revert `180002`. ⚠️ Nunca ligar sem auditar TODOS os lançamentos do projeto antes.
- **Mapa:** +7 categorias (Multas, Aluguel Equip., Refeições, Postais, Mat. Consumo, Seguros→R5, Viagens→R19) `170001`/`190001`. **GDB = folha terceirizada (mão de obra), RECONFIRMADA em R3** pelo Luciano (obs em 2.10.93; não mudar de linha sem nova decisão).
- **DRE UI:** coluna AV% calculada (% da Receita Bruta) em todos os níveis do drill-down (`DreTable.tsx`).
- **PENDENTE — regime de adiantamentos (aguardando Fernanda):** adiantamento 2.08.01 com projeto de linha = despesa no mês do pagamento; NF de baixa com projeto **"Baixa de Adiantamento"** = neutra no DRE. Procedimento: `../PROCEDIMENTO_FERNANDA_ADIANTAMENTOS.md`. ⚠️ **NÃO ligar as regras antes** de a Fernanda criar o projeto e marcar o backlog jan/2026→hoje (senão o histórico dobra). Diferenças por retenção (ISS etc.) são esperadas; DRE contará o líquido pago.

## Módulos (ativos vs stub)

**Ativos (verificado em `app/(hub)/`):**
- `/` — dashboard: cards por canal (faturamento → deduções → M.C.) + gráfico faturamento/M.C. por dia. (`HubMain.tsx`)
- `/dre` — DRE consolidado do mês fechado, com drill-down em 3 níveis (linha → categoria → item).
- `/crons` — painel de saúde dos crons.
- `/marketplaces/{mercado-livre,shopee,tiktok,amazon,shein,magalu,vendas-internas}` — páginas de canal (delegam ao componente `CanalDashboard`).
- `/login` + gate de acesso (ver Convenções / Deploy).

- `/fc-projetado` — **Fluxo de Caixa Projetado: PARCIAL (17/08/2026).** Só a seção **"Recebíveis por plataforma"** existe. **6 de 7 plataformas integradas: Mercado Pago, Shopee, Amazon, SHEIN, Magalu e TikTok** — mas **só 5 somam no total** (R$ 1.232.310,62 em 17/08/2026): o TikTok é `foraDoTotal` (número real, porém bruto, não caixa líquido — ver Integrações). Falta só o **B2B**. **Três formatos de cronograma convivem:** completo (Mercado Pago), **parcial** (Amazon, SHEIN e, desde 19/08, **Shopee** — parte com data, parte sem, via `valorSemData`; a data da Shopee é DERIVADA: entrega real + 8d, ver Integrações) e inexistente (TikTok, Magalu). ⚠️ **Nem toda integrada tem cronograma:** o Magalu informa o valor mas não a data, então entra no total e **fica fora das faixas por prazo** — o rodapé do consolidado diz isso. A UI trata `dias: []` como caso de primeira classe (quatro faixas zeradas leriam como "não tem nada a receber", que é falso). Contrato em `lib/data/types.ts` (`Recebiveis`/`RecebiveisPlataforma`/`RecebivelDia` + `getRecebiveis?()`); roster e ordem da fila em `lib/data/recebiveis.ts` (Mercado Pago → Shopee → TikTok → Amazon → SHEIN → Magalu → B2B); UI em `components/RecebiveisPorPlataforma.tsx` — **barras** (decisão do Luciano 17/08): faixas D+7/D+15/D+30/+30 com barrinha de participação no total, e cronograma dia a dia como barras horizontais escaladas pelo MAIOR dia (piso de 2px para dia pequeno não sumir e virar "zero"); o cronograma abre por padrão quando há dado. Tudo calculado no client a partir de `dias[]`. Para ligar uma plataforma: preencher `total`/`disponivel`/`dias` no `getRecebiveis` do `supabaseProvider` e virar `integrado: true` — a UI não muda. O total do topo soma **só as integradas** e exibe a cobertura ("1 de 7"). **Faltam:** saídas projetadas (contas a pagar Omie) e saldo projetado por dia.

**Stubs (placeholder vazio — verificado: usam `<Placeholder/>`):**
- `/previsao-impostos` — **Previsão de Impostos: NÃO construído.**

## Integrações por canal

Padrão comum: OAuth com `refresh_token` **só no Vault**; `*_oauth_state` (linha `id=1`) guarda `access_token`+`expires_at`; HTTP feito de dentro do Postgres; jobs longos usam `SET statement_timeout`. Detalhe histórico (ML/Shopee) em `APRENDIZADOS_SHOPEE_ML.md`.

- **Mercado Livre** — cron `ml-diario`; ADS via `ml-ads-item`/`ml-ads-reconferir` (captura de D-1 às 03:00 vem parcial → reconferência re-puxa); tarifas de fatura extras em `ml_fatura_tarifas` (assessoria CPAC/Minha página CESM → DRE R6) e `ml_full_ccolpa` (coleta pré-agendada → Full/C7). Régua: comissão = `sale_fee` do pedido, que **é composto por `CVVML + CVVPRC + CVVFNU`** (provado pedido a pedido — não é só CVVML). O que **NÃO** entra no cálculo do billing ML: ver bloco "Billing ML — o que NÃO entra" abaixo. Token: `ml-refresh-token` + Edge Function `ml-token`.
- **Shopee** — cron `shopee-diario` (`shopee_cron_diario`: lista pedidos → detalhe → **escrow** (repasse, fonte de verdade das deduções) → ADS → DIFAL). Escrow re-passa 12h/18h (`shopee-escrow-*`) contra o lag do último dia. ADS reconferido 12h/18h (`shopee-ads-reconferir-*`) — a captura das 03:30 pode falhar/vir parcial.
  - **⚠️ Re-busca do escrow PÓS-CONCLUSÃO (fix 18/08/2026, `20260818150001`):** a 1ª foto do escrow (no READY_TO_SHIP) **não é final** — a comissão de afiliados (`order_ams_commission_fee`) só aparece na resposta perto da conclusão, e o escrow final divergia da 1ª foto em >50% dos pedidos (jul: 3.483/6.048 creditados, repasse **inflado** em R$ 9.337; ago: 807/937, +R$ 4.644 — M.C. otimista). Por isso `ams_commission` zerou de ~11/07 em diante: o backfill de 09/07 (`shopee_fill_ams`) leu o HISTÓRICO já concluído (valores certos, jun R$ 6.569/465 pedidos) e, ao alcançar o tempo real, o fluxo diário passou a fotografar cedo demais e gravar 0 definitivo (`fill_ams` só olha `IS NULL`; ficou OBSOLETA, ver COMMENT). **Fix:** `shopee_refetch_escrow` relê o escrow **uma única vez** quando o pedido ganha `ESCROW_VERIFIED_ADD` na carteira (= conclusão; marcador `shopee_pedidos.escrow_refetch_em` impede loop — pedido ajustado PÓS-verificação diverge da carteira para sempre, categoria ADJUSTMENT_*). Validado ao vivo: escrow relido pós-conclusão = crédito da carteira centavo a centavo. Os jobs `shopee-escrow-meiodia/tarde` rodam `shopee_refetch_escrow_loop()` após o fill (ambos após uma passada da carteira, que é quem revela conclusões). Backfill 18/08: ~3,8k pedidos de 10/07→18/08 re-buscados via job temporário auto-removível `shopee-refetch-backfill`; pré-10/07 e 1ª foto==crédito carimbados sem API. ⚠️ Consequência: números de jul/ago mudaram (deduções maiores, reais). **Correção do seed (19/08, `20260819120001`):** o carimbo "pré-10/07 = final por construção" estava errado para pedidos criados 05→10/07 ainda em trânsito quando o backfill de ams rodou (09/07) — 528 pedidos creditados com escrow bruto e ams=0 (R$ 4.364 de repasse inflado) foram descarimbados e re-buscados. Validação viva da régua: divergência escrow_amount × crédito da carteira zerou pós-10/07.
  - **⚠️ UNPAID congelado (fix 19/08/2026, `20260819120001`):** as fases 1+2 do cron só INSEREM (`ON CONFLICT DO NOTHING`) — pedido que nascia UNPAID e era pago depois ficava UNPAID para sempre: fora da bruta e sem escrow (medido: 27 pedidos creditados na carteira constando UNPAID, **R$ 4.056 invisíveis**, 13/07→18/08; `shopee_reconfere_status` não cobria, só flipa CANCELLED). **Fix:** `shopee_reconfere_unpaid(dias)` re-checa os UNPAID via `get_order_detail` (lotes de 50) e atualiza status+pay_time; roda nos jobs `shopee-ciclo`/`shopee-ciclo-tarde`. 1ª rodada: 190 verificados → 35 pagos + 128 cancelados.
- **TikTok Shop** — cron `tt-diario` (`tt_cron_diario`, 3 fases: order search → `tt_fill_finance` by-order → `tt_fill_affiliate`). **Régua:** `tt_faturamento`/`tt_cmv` contam **pedido pago antes de liquidar** (exclui UNPAID/CANCELLED); receita bruta = `fin_revenue` quando liquidado, senão `payment_total`. **M.C. PROJETADA desde 12/08/2026 (decisão do Luciano — supera o settled-only/piso de 28/07):** com o mix dominado por afiliados+lives, esperar o settlement (que só existe PÓS-ENTREGA) inflava a M.C. do mês corrente em todo dia de venda forte. `tt_deducoes_projetado` = deduções reais nos pedidos liquidados + **provisão por take-rate móvel** (60d de liquidados: comissão ~6,0% + SFP ~6,1% + R$/item ~5,43 + frete ~0,65%, taxas contratuais estáveis) nos demais; a provisão morre **pedido a pedido** quando o statement chega. **Afiliado = REAL em D+0** via `POST /affiliate_seller/202410/orders/search` (`tt_fill_affiliate`: criador + parceiro TAP por pedido em `tt_pedidos.aff_commission`; pedidos na janela sem registro e >24h = orgânicos com 0). **Ads (GMV Max): NÃO provisionado** (decisão 12/08; cobrança em conta de anúncios própria, estilo Amazon Ads) — entra quando conectar a Business API (`tt_ads_diario` pronta, com proteção anti-dupla-contagem vs settlement). Config em `tt_provisao_config` (seeds, mix_cutoff). Card mostra selo "M.C. projetada — X% liquidado". **App: "Hub Financeiro Essenza Inhouse"** (categoria Desenvolvedor inhouse do vendedor, app_key `6kunfi6tu9pf2`, 4 scopes incl. `seller.affiliate_collaboration.read`; o app ISV antigo `6kgr85hkbgv3q` não tinha affiliate no pacote). Migrations `20260812120001/120002/130001`. `tt-semanal` re-sincroniza 30 dias (revisão de cancelados).
- **Amazon** — crons `az-diario`/`az-semanal`.
- **Amazon Ads (integrado 03/08/2026)** — app LwA separada da SP-API, prefixo **`azads_`**. Custódia no Vault (`azads_*`); troca do code **dentro do Postgres** (`azads_exchange_code` + allowlist de redirect); callback principal = Edge **`amazon-ads-callback`** (a rota Vercel `callback-amazon-ads` é alternativa; Vercel segue pausada por cota); gateway de token = Edge **`amazon-ads-token`**. Perfil único BR (`1926422998361173`, BRL, America/Sao_Paulo). **Ingestão:** Reports v3 são assíncronos (~12–15 min de fila) → fila `azads_report_jobs` em 3 estados honestos (`PENDENTE/INGERIDO/FALHOU`) + `azads_gastos` (dia × campanha × produto, **snapshot**: `azads_replace_gastos` substitui a janela — merge deixaria valor morto). Cron `azads-diario` 03:45 BRT (colhe + pede D-3..D-1; flutuação de ~72h coberta pela re-pedida) e `azads-colher` 04:25 BRT; log em `ml_cron_log`. **Validação (item 6): API = painel 0,000%** (julho R$ 14.125,85 exato, cliques 18.927 idênticos). Card lê `azads_ads`; gráfico diário via `dre_diario_canais` (ramo az, full join). Migrations `20260730120001`, `20260803120001/130001/140001/150001`. Histórico API: SP ~95d, SB/SD ~60d — backfill além disso não existe. Detalhe: `AMAZON_ADS_DESCOBERTA.md`.
- **SHEIN (integrada 07/08/2026 — produção, ingestão ATIVA)** — prefixo `shein_`. App "HUB FINANCEIRO" aprovada; loja Brigide s Store autorizada (openKeyId em `shein_oauth_state`, supplier `2630286`, secretKey no Vault, IP `56.126.53.22` na lista branca). **Ingestão** (`20260807170001`): `shein_fill_pedidos(de, ate)` varre janelas de 47h (cap da API: 48h, horários em **UTC+8**/Asia-Shanghai) → `order-list` (queryType=1, pageSize 30) → `order-detail` (lotes de 30) → `shein_pedidos`/`shein_itens` (cada goodsId = 1 unidade). Crons `shein-diario` (03:50 BRT, re-sync 3d) e `shein-semanal` (dom 05:35 BRT, 30d), fases resilientes, log em `ml_cron_log` job `shein_fill`, no catálogo do `crons_status` (`20260807180001`). **Régua do card (v2, 09/08/2026 — decisão do Luciano):** competência `payment_time` BRT; **bruta = preço PAGO pelo cliente** (Σ `sellerCurrencyDiscountPrice` por item; bruta ⊃ devoluções 6/8/9, líquido = bruta − devoluções) — a v1 usava `productTotalPrice` (preço CHEIO de tabela), que dava ago/2026 R$ 53k contra GMV R$ 25,4k da central e CMV% 36% (base inflada). Deduções REAIS por item — Comissão, Taxa de serviço e **Subsídio SHEIN como CRÉDITO** (`(preco − cupom − promo) − preco_com_desconto`; a SHEIN calcula o repasse sobre base MAIOR que o preço pago; ago: R$ 7.754) — e CMV por `sellerSku` (vigência). Cupons/promos da loja não são linha (embutidos no preço pago). **Identidade validada: pago(válidos) − (comissão + taxa − subsídio) = `estimatedGrossIncome` centavo a centavo; a M.C. em reais independe da base.** Divergência residual conhecida vs central (~9%, ago 01-08: 178 ped/23,2k aqui × 185 ped/GMV 25,4k lá): pedidos reembolsados que o order-detail ZERA ou o order-list omite (re-sync manual 09/08 confirmou que a API não os devolve) — conciliação exata na fase do settlement. Migration `20260809130001`. Primeiro pedido da loja: 24/07/2026; backfill completo desde então (jun→14/jul = zero pedidos, loja nova). **Settlement INTEGRADO (10/08/2026, `20260810120001`):** endpoints descobertos via SDK público easycb-go — `POST /open-api/finance/get-check-order-list` (janela <7d por addTime, pageSize≤30, UTC+8; 1 check order Dxxxx/pedido; `checkStatus` 1=aguardando 3=PAGO; `reportOrderNo` Bxxxx = ciclo semanal) e `GET /open-api/finance/get-check-order-detail` (decomposição por item; assinar só o path, query fora da assinatura); `report-list` = 403 (permissão a pedir no portal se necessário). Tabela `shein_settlement` + `shein_fill_settlement(de,ate)`; diário re-varre 14d (captura virada 1→3, pgto ~9d após addTime), semanal 35d (`shein_cron_semanal`, re-agendado). RPC `shein_conciliacao(mes)` = régua × dinheiro. **VALIDADO no 1º repasse real (B2608055555577856, pago 10/08 ~02:04 BRT): 3 pedidos = R$ 456,50, IGUAL à régua v2 centavo a centavo, divergência zero; detalhe do pago com whtTotalAmount/commissionSaleTax/sellerRealTax/GNRE/returnExpense TODOS zero no BR — sem custo oculto.** Print da central (pedido GSH1RW57T002TGM) também bate 1:1 com o card ("Intermediação de frete" da central = nossa "Taxa de serviço"). Pendências SHEIN: ADS se/quando existir; pedir permissão de `report-list` se quisermos o nível ciclo/repasse. **Série diária e DRE por SKU LIGADOS (09/08/2026, `20260809120001`):** ramo `sh` em `dre_diario_canais` (deduções reais do pedido na coluna `comissao`, técnica da Shopee) e em `dre_sku` (valores 100% reais POR ITEM, sem rateio — validado Σ itens = pedido centavo a centavo em ago/2026); card com "Ver mais" e página `/marketplaces/shein` completa (gráfico + SKUs). Modelo de auth ≠ OAuth dos demais: **NÃO existe refresh token**. appid+appSecretKey (Vault `shein_app_id`/`shein_app_secret`) → seller autoriza a app → `tempToken` (curto, uso único) → `shein_exchange_token()` chama `/open-api/auth/get-by-token` (assinatura modo APP) e recebe **openKeyId + secretKey de LONGA DURAÇÃO** (openKeyId → `shein_oauth_state`; secretKey decriptado AES-128-CBC chave=16 primeiros bytes do appSecret, IV `space-station-de` → Vault `shein_secret_key`). Assinatura de API (validada 1:1 contra o SDK oficial `sheinsight/open-sdk-java`): `x-lt-signature = randomKey(5) || base64(hex(hmac_sha256(keyId&timestamp_ms&path, secret||randomKey)))` — timestamp em **milissegundos**; base64 do PG solta `\n` (removido). Edge: **`shein-oauth-callback`** (recebe tempToken, GET ou POST) e **`shein-token`** (gateway x-api-key `shein_token_key`; `?path=` devolve headers assinados). `shein_refresh_token()` é validador honesto de custódia (sem cron keepalive — nada expira). Card no dashboard: bruta, deduções reais e M.C. (`shein_faturamento`/`shein_deducoes`/`shein_cmv`; nota "valores do pedido — settlement na próxima fase"). Fase seguinte: deduções de settlement (conciliação). Migrations `20260807150001_shein_oauth`, `20260807160001_shein_exchange_domain`.
  - **Validado AO VIVO no ambiente de teste (07/08/2026, code 0):** interface de teste = `https://openapi-test01.sheincorp.cn` (chaves fixas por região na página "Loja de teste" do portal; Brasil OpenKey `9F9ADD60...`). Assinatura nossa aceita pelo gateway real. **Réguas do `/open-api/order/order-list` (POST):** body `{queryType, startTime, endTime, page, pageSize}`, datas `YYYY-MM-DD HH24:MI:SS`, **janela máx. 48h** (172800000 ms — paginar por janelas, estilo Shopee); resposta `{code:"0", info:{count, orderList:[{orderNo, orderStatus, orderCreateTime, orderUpdateTime}]}}` — detalhe do pedido vem de endpoint próprio (fase 100%). Autorização (portal "Depuração de autorização"): link `https://<host>/#/empower?appid=..&redirectUrl=<base64>&state=..`; host teste `openapi-sem-test01.dotfashion.cn`, produção `openapi-sem.sheincorp.com`; o tempToken chega no FIM do caminho do redirect (callback trata path/query/POST). IP de egresso do PG (lista branca SHEIN): `56.126.53.22` (re-checar com api.ipify.org se chamadas falharem).
- **Magalu (integrada 17/08/2026 — MVP, ingestão ATIVA)** — prefixo `magalu_`. APIs novas do Magalu Devs (`api.magalu.com/seller/v1/...`), NÃO o legado IntegraCommerce. **Auth = OAuth2 padrão** (id.magalu.com): access_token **2h** em `magalu_oauth_state`, **refresh_token 6 meses de USO ÚNICO** no Vault (`magalu_refresh_token` rotaciona a cada renovação — padrão Tiny, advisory lock `421982799`). ⚠️ **A API Key do ID Magalu NÃO acessa dados de seller** (401 em qualquer formato, prod e HMG — testado exaustivamente 17/08): ela só cria/gerencia clients. O client OAuth **"Hub Financeiro Brigide"** (UUID `0d5992f9-75cd-47a5-a4dc-1d1204f2924f`, em `magalu_oauth_state.client_uuid`) foi criado via **IDM CLI** (`.tools/idm` na pasta-mãe do projeto; login PKCE + consentimento como Pessoa Física), com 8 escopos read-only default (pedidos, entregas, notas, logística, **`open:order-financial-report-seller:read`**, portfólio skus/prices/stocks); consentimento do seller feito como tenant PJ **Essenza Di Chef** (`c75c60f0-e042-493d-acc3-826b143e39e1`, gravado em `tenant_id`; header `X-TENANT-ID` nas chamadas). Edge: **`magalu-oauth-callback`** (troca o code via RPC `magalu_exchange_code`; redirect_uri registrado no client) e **`magalu-token`** (gateway x-api-key `magalu_token_key`). Reautorização: `select magalu_auth_url();` → seller loga como a PJ e consente. **Ingestão** (`magalu_ingest_orders(mes)` → `magalu_ingest_page`): `/seller/v1/orders` paginado (`_offset`/`_limit=100`, filtro `purchased_at__gte/lte`), upsert em `magalu_pedidos`/`magalu_itens`; valores vêm em **centavos com `normalizer`** (÷100). ⚠️ Pegadinha: **SKU/nome do item vêm em `items[].info`**, não em `product` como a doc sugere (fix `20260817144243`). **Régua do card (MVP):** competência `created_at` BRT; bruta = `amounts.total` **PAGO pelo cliente** (pós-desconto — desconto NÃO é dedução, seria dupla contagem; fica informativo em `magalu_deducoes.desconto`); status ≠ `cancelled` (valores: new/approved/cancelled/finished); deduções REAIS por pedido = comissão + frete; CMV por `sku` (vigência, unaccent). Nota do card: "valores do pedido — repasse na fase 100%". Crons: `magalu-diario` (03:50 BRT, mês corrente) + `magalu-token-keepalive` (*/30). **Fase 100% (pendências):** ingestão do relatório financeiro/repasses (escopo já autorizado); série diária (ramo `mg` em `dre_diario_canais`) e DRE por SKU; catálogo do `crons_status`; janela do diário não re-varre mês anterior (virada de mês); conciliação com repasse real. Migrations `20260807224955/225712/230130/230201` (versions com relógio errado do MCP; aplicadas 17/08) + `20260817144243`.
- **Magalu — recebíveis (integrado 17/08/2026, MVP)** — **sem cronograma: o Magalu não publica data de repasse.** A API de **Análise Financeira** existe e o escopo já está autorizado — path exato **`GET /seller/v1/financial-analysis/orders`** (janela máx. **15 dias** por `purchased_at__gte/lte`, ou `order_id` avulso, que aceita só o **code numérico**, não o UUID) — mas ela é **visão contábil por pedido** (CREDIT/DEBIT/INFORMATIVE, comissão, MDR, subsídios), **sem campo de data de pagamento** (só `created_at`/`updated_at`). E hoje **vem vazia**: só devolve pedidos "Entregue"/"Cancelado faturado" a partir de 05/05/2026, e a operação tem 2 pedidos (1 cancelado 30/07, 1 criado 17/08) — testado por janela e por `order_id`, HTTP 200 com `count: 0` em todos. **Régua (MVP):** a receber = Σ (`valor_total` − `comissao` − `frete`) dos pedidos não cancelados — deduções REAIS, vindas do próprio `/seller/v1/orders`. **1ª leitura: R$ 180,43 em 1 pedido.** RPC `magalu_recebiveis()`, migration `20260817202132`; sem cron próprio (o `magalu-diario` já mantém `magalu_pedidos`). ⚠️ **PENDÊNCIA:** não existe sinal de "já repassado" na API, então nada é abatido — correto hoje (operação recém-aberta, nenhum repasse ocorrido), mas **quando o 1º repasse cair é preciso achar onde a API registra isso**, senão o total vira acumulado eterno.
- **SHEIN — recebíveis (integrado 17/08/2026)** — **2º canal com DATA** (o 1º é o Mercado Pago), com cronograma **PARCIAL** como o da Amazon, mas por outro motivo: o check order (`shein_settlement`) traz **`estimate_pay_time`** — a data que a **própria SHEIN** estima para pagar (`check_status` 1 = aguardando, 3 = pago) — só que o check order **nasce quando o pedido chega ao status 5**, ~2 semanas após a venda. Cobertura medida em 17/08: pedidos de 0-7 dias **6%** (4 de 68), 8-14 dias **43%**, 15-21 dias **88%**. **Régua:** COM data = Σ `valor_estimado` dos check orders `check_status=1` por `estimate_pay_time` (BRT); SEM data = Σ `receita_estimada` dos pedidos que ainda não têm check order (receita > 0, status ∉ {6,8,9} = devoluções, janela 60d); total = os dois, **sem sobreposição** (o pedido sai do "sem data" quando ganha check order). **Somar os dois lados é seguro porque são o MESMO número:** nos 121 pedidos que já têm ambos, **120 batem centavo a centavo** (R$ 15.120,62 × R$ 15.322,88); o único divergente (`GSH1R101W000671`, R$ 202,26) é pedido **devolvido depois de pago**. **1ª leitura: R$ 10.667,41 com data (17, 24 e 31/08) + R$ 21.877,81 em 166 pedidos = R$ 32.545,22.** RPC `shein_recebiveis()`, migration `20260817201212`; **sem cron próprio** — `shein-diario`/`shein-semanal` já mantêm pedidos e settlement.
- **Amazon — recebíveis (integrado 17/08/2026)** — **o melhor caso dos quatro: tem valor E data, ainda que parcial.** Fonte: SP-API **Finances v0 `listFinancialEventGroups`**, via Edge nova **`az-recebiveis`** (modos `grupos` e `eventos`; a Amazon só é acessível por Edge — o PG não assina LWA). O saldo vive em **grupos financeiros de ~14 dias**: o grupo **Open** acumula o ciclo corrente; ao fechar ganha `FundTransferDate` + `FundTransferStatus` (**Processing** = dinheiro a caminho do banco, com data · **Succeeded** = já caiu · **Unknown** = saldo negativo/ajuste, não conta). **Régua:** a receber = Σ `OriginalTotal` dos Closed+**Processing** (viram o cronograma) + Σ dos **Open** (valor real, **sem data** — a Amazon não publica quando o ciclo fecha; "~14 dias" é observação, não dado). Negativos entram como estão (débitos que abatem o repasse). **Único canal com cronograma PARCIAL** → campo `sem_data` na RPC e `valorSemData` no contrato; a UI mostra "X sem data prevista" e as faixas por prazo somam só o que tem data. **Objetos:** `az_finance_groups` + `az_fill_recebiveis(dias)` + `az_recebiveis()`; crons `az-recebiveis` (04:40 BRT) e `az-recebiveis-tarde` (13:20 BRT). **1ª carga: 37 grupos → R$ 21.151,13 a caminho (4 grupos, transferência de 17/08) + −R$ 117,77 no ciclo aberto = R$ 21.033,36.** Migrations `20260817200233/200332`. ⚠️ A Edge `az-recebiveis` **não é versionada no repo** (como as demais Edge do projeto). ⚠️ Observar: se um grupo ficar preso em `Processing` sem virar `Succeeded`, ele conta como a receber para sempre — o histórico (18 grupos já Succeeded) indica que o ciclo funciona, mas vale conferir depois de um ciclo completo.
- **TikTok — recebíveis (integrado 17/08/2026, FORA DO TOTAL)** — ⚠️ **o TikTok não tem NEM quando NEM quanto.** *Quando:* pedido não liquidado devolve `total_count=0` e tudo zerado em `/finance/202501/orders/{id}/statement_transactions`; os statements (`/finance/202309/statements`) só nascem DEPOIS de liquidar; `/finance/202309/payments` só lista o já PAGO (1 único statement em `PROCESSING` na varredura, e negativo). *Quanto:* **a plataforma SUBSIDIA** — `fin_revenue` foi **104,39% (jun) e 116,93% (jul)** do que o cliente pagou, e o settlement sai sobre essa base maior; resultado, settlement/pago oscilou **84,6%–98,5% por mês (73%–101% por semana)**. A razão estável é settlement/**revenue** (81,0/84,2/84,1%), mas a revenue só existe ao liquidar. **DECISÃO DO LUCIANO (17/08/2026): card INFORMATIVO, fora do total consolidado** — mostra o bruto REAL pago pelo cliente em pedidos não liquidados (**R$ 41.398,88 em 212 pedidos**, o mais antigo de 29/07) + a faixa histórica de repasse; nada de projetar um número único que a variância não sustenta. Campo `foraDoTotal` no contrato (`types.ts`) + tag "fora do total" na UI; o rodapé do consolidado explica que o valor é bruto, não repasse líquido. RPC `tt_recebiveis()` (migration `20260817...tt_recebiveis`); sem cron próprio — lê de `tt_pedidos`, que o `tt-diario` já alimenta.
  - **⚠️ PENDÊNCIA ABERTA — viés na M.C. PROJETADA do TikTok (achado 17/08/2026, Luciano mandou anotar e seguir):** `tt_take_rates` mede cada taxa dividindo por **`fin_revenue`**, mas `tt_deducoes_projetado` aplica esses percentuais sobre **`payment_total`** — e a revenue é 4%–17% MAIOR que o pago (subsídio). **Backtest em 164 pedidos já liquidados (60d): a fórmula projetou R$ 20.575,08 de líquido contra R$ 23.675,12 reais — 13,09% a menos.** Ou seja, a M.C. projetada do TikTok (decisão de 12/08) está **pessimista** nos pedidos não liquidados. Correção provável: projetar sobre a revenue esperada (medir `revenue/pago`) em vez de sobre `payment_total`. **NÃO alterado — régua do Luciano.**
- **Shopee — recebíveis (integrado 17/08/2026)** — ⚠️ **a Shopee informa QUANTO, não QUANDO.** `get_escrow_detail` traz ~90 campos de valor e **nenhuma data de liberação** (verificado no pedido `260817JF32WA8M`); a carteira só registra o que já caiu. Por isso o card ficou **sem cronograma** de 17 a 19/08 — até a data se provar DERIVÁVEL da conclusão do pedido (ver Fases 0-3 abaixo; estimar pelo lag histórico da VENDA seguiria sendo inventar). **Régua:** a receber = Σ `escrow_adjusted` dos pedidos não cancelados **sem** `ESCROW_VERIFIED_ADD` na carteira, restrito à janela coberta pela carteira (pedido anterior a ela é *desconhecido*, não "não pago"). **⚠️ Três exclusões obrigatórias** (senão pedido em devolução fica pendente para sempre — o furo custou R$ 15.014, 11% do total, com 122 pedidos de +30 dias "esperando" repasse que não vem): `order_status='TO_RETURN'`, pedido com `RETURN_COMPENSATION_SERVICE_ADD` (já pago por outro caminho) e pedido com `ESCROW_VERIFIED_MINUS` sem `ADD` (escrow fechado como débito). O excluído vai para `em_disputa` e aparece na nota do card — não some em silêncio. **Objetos:** `shopee_wallet` + `shopee_fill_wallet(dias)` + `shopee_recebiveis()`; crons `shopee-wallet` (04:30 BRT) e `shopee-wallet-tarde` (13:10 BRT), janela de 14 dias (~45s). **API:** `/api/v2/payment/get_wallet_transaction_list` — **janela máx. 14 dias** (15 → `wallet.time_invalid`), `page_no`/`page_size` com flag `more`. **Primeira carga (90d): 16.948 transações, 173 páginas, 279s → R$ 120.902,72 a receber (716 pedidos), R$ 14.706,17 em disputa (151), saldo em carteira R$ 121.726,83; 98% do pendente em pedidos de até 15 dias.** Migrations `20260817190812/191510/191556`.
  - **⚠️ NÃO PROCURAR DE NOVO: a data de liberação da Shopee não existe em lugar nenhum (investigado a fundo em 17/08/2026, a pedido do Luciano).** Provas: (a) **Open API** — `get_escrow_list` traz `escrow_release_time` mas só do PASSADO (janela futura devolve zero); `get_billing_transaction_info` tem o tipo **1 = "To Release"**, exatamente o conceito certo, mas responde *"only applicable for cross boarder shop"* (nossa loja é local BR), idem `get_payout_detail`; `get_escrow_detail` e `get_order_detail` não têm nenhum campo de liberação (só `edt_to` e `return_request_due_date`). (b) **Portal (Seller Center)** — a tela *Minha Renda → Pendente* é servida por `POST /api/v4/accounting/pc/seller_income/income_overview/get_income_detail` (`income_category:1`), que TEM o campo `income_estimated_escrow_time`, mas a Shopee **não o preenche**: nos 764 pedidos pendentes de 17/08, apenas **1** tinha data (R$ 179,22); os outros 763 vinham com `0`, e a coluna da tela mostra o texto genérico "O depósito normalmente é…". (c) A tela *Meus Recebíveis* (`POST /api/v4/accounting/receivable/enquire_receivables`, `year_month:"202608"`, valores em 1e5) tem `due_date`, mas é a **UR da adquirente → Shopee**, não o escrow → carteira: dos 118 recebíveis de jul+ago, só R$ 13.628,83 têm vencimento futuro (2 dias de horizonte). Conclusão: card da Shopee fica **sem cronograma** por ausência de dado, não por opção.
  - **Validação da régua (17/08/2026):** a própria Shopee diz *Pendente Total* **R$ 125.938,73** (`get_income_overviews`, type 9) e a lista de pendentes soma **R$ 127.275,34 em 764 pedidos**; o Hub diz **R$ 120.902,72 em 716** — **96% de aderência**. Resíduo esperado: pedidos fora da janela de 90 dias da carteira + ajustes. Calibrar os 4% restantes é pendência aberta.
  - **Fase 0+1 do cronograma (18/08/2026) — a data NÃO é publicada, mas É DERIVÁVEL (medido, não teoria):** a liberação do escrow É o momento em que o pedido vira `COMPLETED` — `update_time` do `get_order_detail` = crédito `ESCROW_VERIFIED_ADD` na carteira em **±5min em 99,40% de 14.434 pedidos** (86 restantes = evento posterior deslocou o `update_time`; ZERO liberações atrasadas vs conclusão). O pedido conclui por confirmação do comprador OU auto-conclusão no fim da Garantia Shopee (= **entrega + 7d exatos**: `return_request_due_date` = delivered+7d, validado por tracking). Distribuição entrega→liberação (405 pedidos com tracking): 65,8% em D+0..7 (32,6% já em D+0..1), 27,2% no cluster D+7..8 (auto-conclusão), 7,2% em D+8..10 (processamento em horário comercial + fim de semana), máx. 9,93d — **"entrega + 7~8d" é o teto contratual e o erro é sempre a favor do caixa** (dinheiro chega antes ou no teto). ⚠️ Pegadinhas medidas: `edt_*`/`return_request_due_date` SOMEM da resposta após a conclusão (ingestão usa coalesce, nunca anula); **`edt_to` vem VAZIO nesta loja** (Camada B por estimativa de entrega da Shopee ficou sem dado); `order_status` em `shopee_pedidos` ficava defasado fora da janela de re-sync (pedido liberado constava READY_TO_SHIP — o `em_disputa` operava sobre status velho; corrigido pela Fase 1). **Fase 1 ATIVA:** colunas de ciclo de vida em `shopee_pedidos` (`status_update_time`, `delivered_time` via `get_tracking_info`, `pickup_done_time`, `edt_*`, `return_request_due_date`, `ciclo_atualizado_em`) + `shopee_fill_ciclo(det, trk)` (detail em lotes de 50 sobre os pendentes do recebível + tracking 1/pedido só dos entregues sem data) + crons `shopee-ciclo` (05:00 BRT) e `shopee-ciclo-tarde` (13:40 BRT), depois das passadas da carteira; log `ml_cron_log` job `shopee_ciclo`; no catálogo do `crons_status`. Backfill feito 18/08: 975 pendentes atualizados + 338 entregas reais; ciclo dos 14,4k já liberados copiado para as colunas (base de validação da Fase 3). Cobertura no D0 (% do valor pendente): 8,3% COMPLETED (dinheiro já liberado, carteira confirma no próximo sync), **39,8% entregue com data real de entrega**, 33,6% em trânsito, 18,3% pré-envio. Migrations `20260818130001/130002`.
  - **Fases 2+3 ATIVAS (19/08/2026 — DECISÃO DO LUCIANO: exceção à REGRA DURA, precedente TikTok): cronograma com data DERIVADA, Camada A.** NENHUM valor é estimado (escrow real da API); só a data é derivada de fatos da própria Shopee. **Régua:** pedido `COMPLETED` ainda sem crédito → data = HOJE (dinheiro já liberou, carteira confirma no próximo sync); pedido ENTREGUE (`delivered_time` real) → data = **entrega + 8d** (o teto da conclusão automática com folga de processamento; 2/3 caem ANTES — erro sempre a favor do caixa; nunca no passado); em trânsito/pré-envio → `sem_data` (`valorSemData` na UI, estilo Amazon — ganha data quando entrega; Camadas B/C REJEITADAS: exigiriam estimativa NOSSA de prazo de entrega, já que a Shopee não manda edt). **Fase 3 embutida no RPC (detector de drift, condição de permanência):** a cada leitura mede, nos pedidos creditados nos últimos 30d com entrega conhecida, o % do VALOR liberado até a data prevista +1d; **abaixo de 80% (base mín. 50 pedidos) o cronograma se SUSPENDE sozinho** (dias=[], tudo em sem_data, nota explica) — pega mudança na Garantia Shopee sem intervenção. **1ª leitura (19/08): R$ 63.545,72 com data (51,3% do valor, 384 pedidos, 19→27/08) + R$ 60.314,98 sem data (325) = R$ 123.860,70; Σ dias = com_data centavo a centavo; acurácia 30d medida = 98,36% (196 pedidos), folga de 18pp sobre o piso.** Selo no card: régua + acurácia + base, tudo visível. RPC `shopee_recebiveis()` reescrita (novos campos `dias`/`com_data`/`sem_data`/`cronograma_ativo`/`acuracia_30d`/`base_acuracia`), provider e roster atualizados; UI intocada. Migration `20260819170001`.
  - **Fases 4+5 (20/08/2026) — conciliação 1:1 com o portal FEITA, resíduo NOMEADO, régua corrigida.** Lista de pendentes do Seller Center (721 pedidos, R$ 127.190,94; card "Pendente Total" R$ 126.576,67) × Hub (660 + 164 disputa), pedido a pedido: **617 batem**; 90 "só no portal" (R$ 17,2k) = pedidos de 20-21/08 **ainda não ingeridos** (lag intradiário do cron, se resolve sozinho na madrugada — a Shopee roda em UTC+8, pedido "de amanhã" existe desde as 13h BRT); 28+4 "só no Hub" (R$ 5,4k) = concluíram/liberaram horas antes do nosso sync (timing espelhado); 14 (R$ 2,5k) = nossa exclusão em_disputa (sinais da carteira, mais fresca que o portal). **BUG REAL corrigido (`20260820190001`): "Reembolso por objeto perdido"** — pedido extraviado é pago via crédito MONEY_IN com `transaction_type` VAZIO na carteira (as únicas linhas de tipo vazio: 24 pedidos, R$ 3.663,65); a régua não reconhecia e 7 pedidos (R$ 1.015,27, desde junho) ficavam "a receber"/fixos em "hoje" PARA SEMPRE. Fix: tipo vazio + MONEY_IN = compensação → em_disputa. **Conclusão: aderência com o portal é ~100% módulo timing intradiário — não existe mais resíduo estrutural.** Becos (Fase 4): (a) `income_estimated_escrow_time` segue morto — 29/721 (4%) preenchidos em 20/08, monitorar de vez em quando; (b) export XLSX da Minha Renda não testado; (c) ticket para o gerente Shopee redigido (pedir "To Release" para loja local) — com o Luciano; (d) changelog da Open API sem novidade de payout local. **Receita do portal p/ reconferências futuras** (sessão logada do Seller Center): `GET /api/v4/accounting/pc/seller_income/income_overview/get_income_overviews?SPC_CDS=<cookie SPC_CDS>&SPC_CDS_VER=2` (type 9 = Pendente Total, valores em 1e5) e `POST .../get_income_detail?SPC_CDS=...&SPC_CDS_VER=2` body `{"source_type":0,"income_category":1,"pagination_info":{"direction":0,"limit":100}}`, paginação por `data.next_page` (GET sem SPC_CDS = 404; POST sem body certo = code 30000 "unknown system error").
- **Mercado Pago (integrado 17/08/2026 — recebíveis do F.C. Projetado)** — prefixo `mp_`. ⚠️ **NÃO tem app, OAuth, Vault nem keepalive próprios: usa o access_token do MERCADO LIVRE.** `api.mercadopago.com` aceita o token de `ml_oauth_state` (mesma conta — `GET /users/me` → 200, user `494625660` BRIGIDE STORE/MLB, testado 17/08/2026). A ingestão pega o token por `ml_get_state()`; se o ML for reautorizado, o MP acompanha de graça. **Régua do recebível (dinheiro, não competência):** pagamento `status=approved` + `money_release_status='pending'` + `money_release_date >= hoje` (BRT); valor = **`transaction_details.net_received_amount` − `transaction_amount_refunded`**. `net_received_amount` é o líquido REAL que cai na conta — provado no pagamento `172744564388` via `charges_details`: 199,90 − 9,87 (`ml_sale_fee`) − 25,55 (`shp_fulfillment`) − 0,07 (`mp_processing_fee`) − 9,99 (`coupon_fee`) = **154,42, centavo a centavo**. Nada estimado (REGRA DURA): o número vem pronto da API; `charges` guarda a composição por pagamento (sem PII do comprador). **API:** `GET /v1/payments/search?range=money_release_date&begin_date=NOW-3DAYS&end_date=NOW+120DAYS&status=approved&limit=100&offset=N` — aceita **offset alto** (testado 7000, sem cap) então varre a janela inteira sem janelar por data; a cauda de liberação termina dentro de **90 dias** (total idêntico em +90d/+180d/+365d/+730d), 120d é folga. **Objetos:** tabela `mp_pagamentos`, ingestão `mp_fill_recebiveis(dias, max_paginas)` (advisory lock, log em `ml_cron_log` job `mp_recebiveis`), leitura `mp_recebiveis()` (total + valor por dia + `sem_liquido` como cobertura honesta). Crons `mp-recebiveis` (04:15 BRT, depois do ml-diario) + `mp-recebiveis-tarde` (13:00 BRT — decisão do Luciano 17/08: a foto da madrugada envelhecia durante a tarde; custo medido 100s/execução, sem throttle). **Primeira carga 17/08/2026: 9.011 pagamentos, R$ 1.057.648,89 a receber, 7.120 pendentes, `sem_liquido` = 0.** Migrations `20260817182655/183055/183245`. **Pendências:** saldo já liberado na conta (`/users/{id}/mercadopago_account/balance` → **403** e `/v1/account/balance` → 404 com esta credencial; card mostra `disponivel: null`); conferir o total contra a tela "Seus recebimentos" do MP; relatório de liberações (`/v1/account/release_report`, assíncrono) se quisermos conciliar o que já caiu.
- **B2B (Tiny)** — notas fiscais do Tiny (`b2b_notas`/`b2b_itens`). Discriminador do canal B2B (o `/notas` do Tiny devolve TODAS as NFs, marketplace incluso): **`ecommerce` vazio + `tipo` "S" + `naturezaOperacao` "Venda%"** (exclui devolução de compra e remessa/bonificação). Ingestão: `b2b_fill_notas(de, ate, off_start, max_pages)` — em lotes. **NÃO tem cron: ingestão manual** (só `tiny-token-keepalive` existe). (Verificado 28/07/2026.)
- **Omie (despesas → DRE Grupo R)** — cron `omie-diario` (`omie_fill_despesas`: re-sync completo das contas a pagar) + `omie-semanal` (conta corrente `omie_fill_movimentos` + fornecedores). Classificação do DRE: linha vem do **PROJETO** (`omie_projeto_dre`), fallback **categoria** (`omie_dre_mapa`); include/exclude por categoria. **PIS/COFINS** (2.06.03→I3, 2.06.04→I4) e **DIFAL** (2.06.94→I1) entram acima da M.C.; competência dos impostos = mês do vencimento −1. Detalhe no Notion.
  - **[EM REVISÃO] PIS/COFINS entra acima da M.C. desde `91b76b3` (23/07/2026).** Supera a decisão anterior de deixar a carga tributária completa (PIS/COFINS/ICMS próprio/IRPJ/CSLL) para o módulo de Impostos futuro. **EM REVISÃO — não alterar sem decisão do Luciano.**
  - **Processo humano (origem da classificação do DRE):** a **Fernanda concilia** todos os gastos no Omie; o **Diego classifica cada lançamento do Omie numa linha do DRE** (e confere os canais contra os números que já tem). O Hub automatiza essa classificação via `omie_projeto_dre`/`omie_dre_mapa`. ⚠️ A régua da equipe no ML é **caixa/fatura do mês, não competência** — origem de divergência (ver Convenções). Fonte: página "DRE:" do Notion (13/07/2026).

### Billing ML — o que NÃO entra no cálculo (sub_types)

⚠️ **Não recontar.** 3 dos 4 maiores "créditos ignorados" do ML eram dupla contagem ou pass-through. **Achar um crédito é fácil; provar que ainda não está contado é o trabalho** (metodologia em `LICOES.md`).

- **`CFONPN` (taxa de parcelamento) — NÃO entra.** Pass-through net-zero: equivale ao acréscimo que o comprador pagou; receita e taxa se anulam. Ingerir só o custo deixaria a M.C. ~R$154.775 **pessimista**.
- **`CVVPRC` + `CVVFNU` — NÃO entram.** Já dentro do `sale_fee` (`sale_fee = CVVML + CVVPRC + CVVFNU`, provado pedido a pedido). Entrar = dupla contagem ~R$40k.
- **`BVVML` / `BFFE` / `BVVPRC` / `BVVFNU` (estornos) — NÃO entram.** A régua `paid + partially_refunded` já trata (venda cancelada sai inteira); creditar o estorno por cima = dupla contagem.
- **`CXDE` — é FRETE, não fricção.** O ML migrou `CFFE`→`CXDE` ~24/06/2026; o frete do Hub vem da API de shipments. Entrar = +R$61k de dupla contagem.
- **`BPAD` — ENTRA, credita o ADS.** `ADS líquido = ADS bruto − BPAD` (o gasto vem bruto da API de delivery; o ML bonifica parte na fatura).
- **`CV` — AGUARDANDO VERIFICAÇÃO.** Confirmar por pedido se está dentro do `sale_fee` **antes** de decidir; **não excluir por materialidade** (ver Pendências).
- Já tratados (o "bloco 7" do billing): `CFWA`/`CFCBE`→Full, `CPAC`/`CESM`→R6 (`4a60520`), `CCOLPA`→Full/C7 (`fd93041`).

## Deploy

- Vercel, projeto `hub-financeiro`, auto-deploy do `main` via integração GitHub. Não há `vercel.json` nem `.vercel/` no repo (verificado).
- URL: `hub-financeiro-omega.vercel.app`
- **Verificado em 28/07/2026: conta Vercel PAUSADA.** Produção indisponível, builds não disparam. Motivo não apurado.
- Último deploy de produção: `2a063bd` (16/07/2026). Ao despausar, o próximo build salta para o HEAD atual — os commits acumulados de uma vez.
- **DEPENDÊNCIA:** `ML_REDIRECT_URI` aponta para `{URL}/api/auth/callback`. Enquanto pausado, **não é possível reautorizar o Mercado Livre**. O refresh diário **NÃO** é afetado (roda na Edge Function `ml-token`, no Supabase).
- **GATILHO:** antes de reautorizar o ML, **despausar a Vercel**. Sem isso o callback não responde.
- **Uso operacional:** **apenas local** — app no Dock apontando para `localhost:3000`; ninguém acessa a URL de produção. O deploy existe hoje só como callback do OAuth ML. (Confirmado 28/07/2026.)

> **Regra:** ao despausar a Vercel, atualizar esta seção com a data.

## Convenções

- 🛑 **REGRA DURA — nenhum custo ou dedução é estimado, arbitrado ou preenchido por percentual. Tudo vem da API.** Se o dado ainda não existe, o número **não é exibido como se existisse** — mostra-se a **cobertura** (quantos pedidos têm o dado real), nunca uma aproximação. (Luciano, 28/07/2026.) **⚠️ EXCEÇÃO desde 12/08/2026 (decisão do Luciano): o TIKTOK usa M.C. PROJETADA** — provisão por take-rate móvel medido sobre os próprios liquidados (não % arbitrado), substituída pelo real pedido a pedido, com selo explícito "X% liquidado / restante provisionado" no card. Motivo: no TikTok o settlement só existe pós-entrega e o mix de lives/afiliados fazia a M.C. do mês corrente inflar dias seguidos. Shopee segue na REGRA DURA (escrow chega no READY_TO_SHIP, sem lag relevante).
- **Piso de cobertura (exibição da M.C.)** — operacionaliza a REGRA DURA. `PISO_COBERTURA = 0.80` em `lib/data/supabase.ts`, hoje **só Shopee** (TikTok saiu do piso em 12/08 → M.C. projetada, acima). Cobertura = **ponderada por receita** (Σ receita liquidada / Σ receita paga), não por contagem de pedidos.
  - **Abaixo do piso:** o card **não exibe M.C.** — mostra bruta, CMV e margem bruta (todos reais, medidos 100% desde a venda), deduções "aguardando liquidação", e "cobertura X% — em consolidação" no lugar da M.C. Nunca apaga o canal, nunca inventa número.
  - **Acima do piso:** a M.C. sai sobre o **subconjunto liquidado dos DOIS lados** (receita liquidada − deduções liquidadas − CMV liquidado). **Nunca líquido cheio contra dedução parcial** — isso é o mesmo descasamento do `+R$1.095` do TikTok, só menor: a 80% de cobertura ~1/5 da receita fica sem custo (no TikTok pode passar de R$500 num mês cuja M.C. inteira oscila nessa ordem). A 100% os dois caminhos coincidem, então a diferença só existe **na faixa onde o erro passaria batido**.
  - **80% é valor INICIAL por julgamento, NÃO calibrado** — revisar após um ciclo completo com variância medida.
  - **Por que não extrapolar o subconjunto:** liquida primeiro quem é mais **antigo** e tem **menos devolução** → o subconjunto liquidado é enviesado pra cedo; escalá-lo para o mês inteiro seria inventar. Por isso mostra-se cobertura, não projeção.
- **CLAUDE.md é a fonte de verdade; Notion é espelho.** Ao mudar regra/arquitetura, atualizar aqui primeiro.
- **Toda sessão termina com commit + push** e confirmação por escrito (nº de commits, SHA do HEAD remoto, working tree limpo/sincronizado).
- **Conflito de informação:** reportar o conflito ao Luciano; **não escolher sozinho** qual está certa.
- **`proxy.ts` na raiz é o correto no Next 16 — substitui o `middleware.ts`. A ausência de `middleware.ts` é ESPERADA, não é bug.** Exporta `proxy(req)` + `config` (matcher). Faz o gate de acesso: em produção exige o cookie `hub_auth`; em `localhost` libera sem gate.
- **`AGENTS.md`** (importado no topo deste arquivo) = regras do **Next 16** para agentes (ler `node_modules/next/dist/docs/` antes de escrever código Next). **`README.md`** é boilerplate do create-next-app (ignorar).
- **`APRENDIZADOS_SHOPEE_ML.md`** = guia histórico de ingestão **só de ML e Shopee**. Útil para as "pegadinhas" de auth/escrow/billing, mas **incompleto/defasado** — ver divergências abaixo. Não copiar cego.

- **Atribuição por COMMIT, não por memória da conversa.** Esta conversa atravessa vários dias e é compactada — **"esta sessão" e "hoje" NÃO são confiáveis**. Trabalho realizado e autorização se provam por **commit (SHA + data)**. Ao afirmar que algo foi feito ou pedido, citar o SHA e a data; se não achar registro, dizer isso.
- **A equipe fecha por CICLO DE FATURA; o Hub fecha por MÊS-CALENDÁRIO.** Não é erro de nenhum lado — é régua diferente (a equipe pega o ML por **caixa/fatura**; o Hub por **competência/mês**), e explica a maior parte das divergências.
- **Toda mudança de régua exige backfill.** Ao mudar uma régua, perguntar: *que dado já ingerido está na régua antiga, e a janela do cron alcança ele?* Se não alcança, backfill **não é opcional** — a mudança da régua da bruta da Shopee (13/07) deixou 01–06/07 órfão, faltando 56% dos pedidos.
- **`LICOES.md`** = lições de metodologia + achados (detectores CMV/bruta ~68% e "0-cancelados"; 3 estados da honestidade no log; bug "gatilho que nunca re-tenta"; `CDLIT`=Seguidores). Números lá são referência histórica, não verdade viva.

### Divergências APRENDIZADOS × código (reconciliado 28/07/2026)
- Cobre **só ML e Shopee**. **Não** contém TikTok, Amazon, B2B/Tiny nem Omie/DRE de despesas (todo o motor de despesas, impostos e DRE consolidado).
- Descreve TikTok como inexistente; a régua atual do TikTok ("pago antes de liquidar", 24/07/2026) é posterior.
- Não reflete o DRE por Omie (Grupo R, PIS/COFINS I3/I4, DIFAL, tarifas de fatura ML, CCOLPA).

## Segredos & hardcodes

- Segredos reais: **Vault** (Supabase) + `.env.local` (gitignored: `SUPABASE_SERVICE_ROLE_KEY`, `ML_CLIENT_SECRET`, `SITE_ACCESS_CODE`, `SUPABASE_URL`).
- **Hardcoded no código** (não-segredo, mas fixo): código de acesso do site **`"1914"`** (fallback de `SITE_ACCESS_CODE` em `proxy.ts` e `app/api/login/route.ts`), `SUPABASE_URL`, `ML_CLIENT_ID`, `ML_REDIRECT_URI`.

## Pendências conhecidas (28/07/2026)

- **43 commits** foram pushados para `origin/main` em 28/07/2026 (HEAD remoto `a0e887d`) — repo sincronizado.
- Módulo stub: **Previsão de Impostos** (em desenho). **F.C. Projetado** saiu do stub em 17/08/2026 — seção "Recebíveis por plataforma" com **Mercado Pago integrado** e as outras 6 plataformas na fila (ver Módulos e Integrações).
- **Recebíveis — validação humana pendente (Mercado Pago):** conferir o total do card contra a tela "Seus recebimentos"/extrato futuro do MP antes de usar o número para decisão de caixa. E **saldo já liberado na conta não existe no card** (endpoint de balance dá 403 com a credencial atual).
- **Cron de ingestão do B2B/Tiny** (hoje manual via `b2b_fill_notas`).
- **DAS/IR** (impostos sobre receita além de PIS/COFINS) — ainda não capturados.
- **Vercel PAUSADA** (ver Deploy) — reautorização do ML bloqueada até despausar.
- ✅ **RESOLVIDO (28/07/2026) — Régua das deduções dos não-liquidados (TikTok e Shopee).** Decisão do Luciano: **nada estimado** (REGRA DURA). **TikTok:** drift removido, `tt_deducoes` de volta a settled-only + piso de cobertura (`20260728120001`). Teste ao vivo provou que o dado de dedução não existe antes de liquidar (order detail sem `fee_breakdown`). **Shopee:** hipótese do "escrow ignorado" **descartada** — varredura 28/07 mostrou **100% de cobertura de escrow em jun e jul, zero ocorrências**; a assimetria teórica (bruta conta os não-cancelados, dedução só quem tem escrow) **não morde** porque o escrow chega no `READY_TO_SHIP`. Blindagem preventiva do `SUM(NULL)→0` na régua viva (`sp_repasse`) + cobertura, régua inalterada (`20260728120002`). Órfãs `sp_comissao`/`sp_frete` marcadas com `COMMENT ON FUNCTION` (não alimentam o app).
- **Rotina de diff repo↔banco (proposta, não implementada):** comparar `pg_get_functiondef` de cada função contra o texto versionado nas migrations e **alertar quando divergirem** — a convenção "atribuição por commit" **não pega** drift aplicado direto no banco (foi assim que o `tt_deducoes` divergiu sem SHA).
- **Conciliação bancária = processo MANUAL (não há módulo).** Extrai do Omie em `.xls` (+ extrato OFX) → concilia fora → devolve ao Omie em `.xls`; envolve a **Cristiane**. A automação (retirada/envio do xls via token/refresh no Supabase) nunca saiu do papel — por isso o módulo de Conciliação Bancária não existe. (Página "CONCILIAÇÃO BANCÁRIA:" do Notion.)
- ✅ **RESOLVIDO (03/08/2026) — Amazon ADS deixou de ser R$0 hardcoded.** Advertising API integrada de ponta a ponta: OAuth autorizado (callback Edge `amazon-ads-callback`, plano B da Vercel pausada), **item 6 bateu 0,000%** (API R$ 14.125,85 = painel R$ 14.125,85, julho/2026, cliques idênticos 18.927), julho backfillado em `azads_gastos` e card + DRE topo + gráfico diário ligados no dado real. Crons `azads-diario`/`azads-colher` ativos. Detalhe completo em `AMAZON_ADS_DESCOBERTA.md`. Pendência residual: comparar relatório × **fatura** (impostos) quando a primeira fatura com ADS fechar.
- **`CV` (billing ML)** — verificar por pedido se está dentro do `sale_fee` **antes** de excluir; não por materialidade (ver bloco "Billing ML — o que NÃO entra").
- **Detector de "dia com 0 cancelados" NÃO construído** — pegaria buracos de captura semanas antes (ver `LICOES.md`).
- **Bug latente:** DIFAL Shopee grava `create_date` em **UTC**, não BRT — cobrança no último dia do mês à noite cai no mês seguinte. Fix: `AT TIME ZONE 'America/Sao_Paulo'`.
- **Robustez do cron da Amazon — AUDITADA em 19/08/2026, sem bug de re-tentativa:** a fila de confirmação (`az_pendentes_comissao`) não tem janela — todo não-confirmado re-entra em cada passada (diário+semanal), então nada fica preso por design. O que existe é lag DA AMAZON: 5 pedidos de julho (Easy Ship/MFN, todos R$ 139,90) seguem sem `ShipmentEventList` na Finances API (só o frete `MFNPostageFee` −19,95, já confirmado) — 1 é `ReturnedToSeller` (comissão pode nunca postar, e a bruta conta a venda), 1 parado em `PickedUp` desde 16/07 (extravio provável), 3 `Delivered` sem evento. Enquanto a Amazon não postar, o card usa a comissão ESTIMADA desses 5 (~R$ 84). ⚠️ Observar: se `ReturnedToSeller`/extravio nunca gerarem evento nem refund, esses pedidos merecem tratamento próprio quando houver volume. `az_settlement` incompleto (3/7) segue pendência separada.
- Menores: `seguidores` sem conferência de contagem; root-cause do timeout do `reconferirDia` (ML, paginar por pendentes).
